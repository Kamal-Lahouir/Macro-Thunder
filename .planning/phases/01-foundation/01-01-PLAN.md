---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - CLAUDE.md
  - src/macro_thunder/__init__.py
  - src/macro_thunder/models/__init__.py
  - src/macro_thunder/persistence/__init__.py
  - src/macro_thunder/ui/__init__.py
  - src/macro_thunder/engine/__init__.py
  - tests/__init__.py
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04

must_haves:
  truths:
    - "Package can be installed with `pip install -e .` without errors"
    - "All submodule directories exist with __init__.py files"
    - "CLAUDE.md documents the DPI-first constraint and pyqtdarktheme package name rule"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project build config with all Phase 1 dependencies declared"
      contains: "PyQt6>=6.4, pynput>=1.7, PyQtDarkTheme-fork>=2.3.4"
    - path: "src/macro_thunder/__init__.py"
      provides: "Package root"
    - path: "CLAUDE.md"
      provides: "Project-specific rules for Claude executors"
      contains: "DPI awareness must be first"
  key_links:
    - from: "pyproject.toml"
      to: "src/macro_thunder"
      via: "hatch.build.targets.wheel packages"
      pattern: "packages.*src/macro_thunder"
---

<objective>
Create the complete project scaffold: pyproject.toml with all dependencies, the src-layout package skeleton with all submodule __init__.py files, and CLAUDE.md with project-specific rules.

Purpose: Every subsequent plan needs an installable package with the correct directory structure. This is the prerequisite for all other Phase 1 work.
Output: Installable package skeleton, CLAUDE.md rules file.
</objective>

<execution_context>
@C:/Users/AMD/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/AMD/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pyproject.toml and package skeleton</name>
  <files>
    pyproject.toml
    src/macro_thunder/__init__.py
    src/macro_thunder/__main__.py
    src/macro_thunder/models/__init__.py
    src/macro_thunder/persistence/__init__.py
    src/macro_thunder/ui/__init__.py
    src/macro_thunder/engine/__init__.py
    tests/__init__.py
  </files>
  <action>
Create `pyproject.toml` at project root using hatchling build backend:

```toml
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "macro-thunder"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "PyQt6>=6.4",
    "pynput>=1.7",
    "PyQtDarkTheme-fork>=2.3.4",
]

[tool.hatch.build.targets.wheel]
packages = ["src/macro_thunder"]

[project.scripts]
macro-thunder = "macro_thunder.__main__:main"
```

Create all __init__.py files (all empty or with a single version string for the root):

- `src/macro_thunder/__init__.py` — contains `__version__ = "0.1.0"` only
- `src/macro_thunder/__main__.py` — placeholder only: `# Entry point — implemented in Plan 03`
- `src/macro_thunder/models/__init__.py` — empty
- `src/macro_thunder/persistence/__init__.py` — empty
- `src/macro_thunder/ui/__init__.py` — empty
- `src/macro_thunder/engine/__init__.py` — empty (reserved for Phase 2)
- `tests/__init__.py` — empty

Do NOT yet add any implementation code to `__main__.py` — that is Plan 03's task. The placeholder comment is sufficient so the entry point exists for `pip install -e .` to succeed.

Run `pip install -e .` after creating the files to verify the package installs correctly. If `PyQtDarkTheme-fork` fails to install (it may on some systems), install `PyQt6` and `pynput` separately and note the fallback in CLAUDE.md.
  </action>
  <verify>
    <automated>cd "C:/Users/AMD/Documents/programming/Macro-Thunder" && python -c "import macro_thunder; print(macro_thunder.__version__)"</automated>
    <manual>Confirm output is `0.1.0` with no import errors</manual>
  </verify>
  <done>
    `python -c "import macro_thunder"` succeeds without errors. `pyproject.toml` exists with all three dependencies. All 7 `__init__.py` files exist in the correct directories.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CLAUDE.md with project rules</name>
  <files>
    CLAUDE.md
  </files>
  <action>
Create `CLAUDE.md` at project root. This is the authoritative rules file for all future Claude executors on this project. Include:

```markdown
# Macro Thunder — Project Rules for Claude

## Critical: DPI Awareness

`ctypes.windll.shcore.SetProcessDpiAwareness(2)` MUST be the first two executable lines
of `src/macro_thunder/__main__.py` — before ANY import that transitively imports PyQt6.

If this call appears after any Qt import, mouse coordinates at 125%+ display scaling
will be wrong (divided by the scale factor). This is NOT a test concern — it's a
runtime correctness issue that only manifests at non-100% display scaling.

## Package Name Warning

Install `PyQtDarkTheme-fork` (NOT `pyqtdarktheme`). Both expose `import qdarktheme`
but the original `pyqtdarktheme` is unmaintained and may fail with PyQt6.

Both cannot be installed simultaneously — the last-installed wins the `qdarktheme`
namespace. Only `PyQtDarkTheme-fork>=2.3.4` is supported.

## Threading Rule

pynput listener callbacks MUST NEVER touch Qt objects directly. Use `queue.Queue` +
`QTimer` drain pattern exclusively. Violation causes unpredictable crashes.

## Data Model Rule

`MacroDocument.blocks` is ALWAYS a flat list of `ActionBlock`. Grouping of consecutive
MouseMove blocks is view-layer only (Phase 3). Never add nested/grouped structures to
the data model.

## Timing Rule

Playback uses absolute `time.perf_counter()` targets — never per-event `time.sleep()`.
Sleep-based playback drifts under scheduler load.

## PyQt6 API Notes

- Do NOT use `Qt.AA_EnableHighDpiScaling` — removed in Qt6, raises AttributeError
- Use `event.position()` / `event.globalPosition()` not `event.pos()` / `event.globalPos()`
- Use `QCursor.pos()` for screen-absolute coordinates in the status bar readout
- Enable mouse tracking on BOTH the main window AND any nested widget that fills the area

## Project Structure

```
src/macro_thunder/
    __init__.py         # version only
    __main__.py         # entry point; DPI call FIRST, then all other imports
    models/             # ActionBlock dataclasses + MacroDocument
    persistence/        # JSON serializer (save/load)
    ui/                 # QMainWindow + panel widgets
    engine/             # Playback engine (Phase 2+)
tests/                  # pytest tests
```

## Block Type Serialization

All ActionBlock subclasses declare `type` as `field(default="...", init=False)`.
This allows `block_from_dict` to reconstruct blocks by removing `"type"` from kwargs
before calling `cls(**kwargs)`. Never change `type` to an `init=True` field.
```

After creating the file, verify it is well-formed by reading it back.
  </action>
  <verify>
    <automated>cd "C:/Users/AMD/Documents/programming/Macro-Thunder" && python -c "import pathlib; content = pathlib.Path('CLAUDE.md').read_text(); assert 'SetProcessDpiAwareness' in content; assert 'PyQtDarkTheme-fork' in content; print('CLAUDE.md OK')"</automated>
  </verify>
  <done>
    `CLAUDE.md` exists at project root and contains the DPI awareness rule, the PyQtDarkTheme-fork package name rule, the threading rule, and the data model flat-list rule.
  </done>
</task>

</tasks>

<verification>
1. `python -c "import macro_thunder; print(macro_thunder.__version__)"` prints `0.1.0`
2. `pip show macro-thunder` lists the package as installed
3. All directories exist: `src/macro_thunder/models/`, `src/macro_thunder/persistence/`, `src/macro_thunder/ui/`, `src/macro_thunder/engine/`, `tests/`
4. `CLAUDE.md` exists and contains DPI and threading rules
</verification>

<success_criteria>
Package installs successfully with `pip install -e .`. All submodule directories have `__init__.py` files. CLAUDE.md exists with critical project rules documented.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` using the summary template.
</output>
