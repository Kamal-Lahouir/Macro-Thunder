---
phase: 09-qa-pass
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/macro_thunder/engine/__init__.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Label/Goto loop executes correctly and loop detection fires after 1000 non-progress iterations"
    - "LoopStart N=3 executes body exactly 3 times"
    - "Dead duplicate LoopStart/LoopEnd handler block is removed from engine"
  artifacts:
    - path: "src/macro_thunder/engine/__init__.py"
      provides: "Single LoopStart/LoopEnd handler (duplicate removed)"
      contains: "# --- Flow control: LoopStart ---"
  key_links:
    - from: "src/macro_thunder/engine/__init__.py"
      to: "LoopEnd handler"
      via: "Single handler at lines 180-202; second identical block at 233-255 deleted"
      pattern: "isinstance.*LoopEndBlock"
---

<objective>
Remove the dead duplicate LoopStart/LoopEnd handler block in engine/__init__.py and verify that flow control (Label/Goto and LoopStart/LoopEnd) executes correctly.

The duplicate block (lines 233-255) is unreachable code — the first handler at lines 180-202 already `continue`s before reaching it. It is a code smell that will confuse future maintenance. Removal is safe with no behavior change.

After removing dead code, run the existing tests to confirm the engine still passes. No new tests needed for the removal itself — the duplicate code is already proven dead by the first `continue`.

Purpose: Clean up the engine so future bug fixes work from a single source of truth for loop handling.
Output: engine/__init__.py with dead block removed; existing tests still passing.
</objective>

<execution_context>
@C:/Users/AMD/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/AMD/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

<interfaces>
<!-- engine/__init__.py dead code to remove -->
<!--
The FIRST LoopStart handler is at approximately line 180:
    # --- Flow control: LoopStart ---
    if isinstance(block, LoopStartBlock):
        progress_since_last_goto = True
        goto_fire_count.clear()
        loop_stack.append((i, block.repeat - 1))
        i += 1
        continue

The FIRST LoopEnd handler follows at ~line 188:
    # --- Flow control: LoopEnd ---
    if isinstance(block, LoopEndBlock):
        ...
        continue

After the WindowFocusBlock handler (~line 231), there is a SECOND identical
LoopStart block (~line 233) and SECOND identical LoopEnd block (~line 241).
These are dead code — delete them both (lines 233-255 approximately).
The WindowFocusBlock handler already does `continue` so control never reaches them.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove duplicate LoopStart/LoopEnd handlers from engine</name>
  <files>src/macro_thunder/engine/__init__.py</files>
  <action>
    Read engine/__init__.py in full. Locate the two sections that are identical:
    - FIRST occurrence: after GotoBlock handler, labeled "# --- Flow control: LoopStart ---" at approximately line 180
    - SECOND occurrence: after WindowFocusBlock handler, labeled "# --- Flow control: LoopStart ---" at approximately line 233

    Delete the SECOND occurrence only (both the LoopStart block and the LoopEnd block that follows it, approximately lines 233-255). The comment, the isinstance check, and all the body lines inside both duplicate if-blocks must be removed.

    After deletion, the order of handlers in the playback loop must be:
    1. LabelBlock
    2. GotoBlock
    3. LoopStartBlock (KEEP this one)
    4. LoopEndBlock (KEEP this one)
    5. WindowFocusBlock
    6. Normal action blocks (timing + dispatch)

    Do NOT change the kept handler logic. Do NOT reformat surrounding code.
  </action>
  <verify>
    <automated>python -m pytest -k "loop or engine or playback" --tb=short -q</automated>
  </verify>
  <done>grep confirms only one "Flow control: LoopStart" comment exists in engine/__init__.py. All existing tests pass.</done>
</task>

</tasks>

<verification>
python -m pytest --tb=short -q
grep -n "Flow control: LoopStart" src/macro_thunder/engine/__init__.py  # must show exactly 1 line
grep -n "Flow control: LoopEnd" src/macro_thunder/engine/__init__.py    # must show exactly 1 line
</verification>

<success_criteria>
- engine/__init__.py contains exactly one "# --- Flow control: LoopStart ---" comment
- engine/__init__.py contains exactly one "# --- Flow control: LoopEnd ---" comment
- Full test suite passes with no new failures
</success_criteria>

<output>
After completion, create `.planning/phases/09-qa-pass/09-02-SUMMARY.md`
</output>
