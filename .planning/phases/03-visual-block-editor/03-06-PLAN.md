---
phase: 03-visual-block-editor
plan: 06
type: execute
wave: 4
depends_on:
  - 03-04
  - 03-05
files_modified:
  - src/macro_thunder/ui/main_window.py
autonomous: false
requirements:
  - LIB-01
  - LIB-02
  - LIB-03
  - EDIT-01

must_haves:
  truths:
    - "Loading a macro (File > Open or library Load) populates the EditorPanel block table"
    - "Saving a macro (File > Save or library Save) persists the current MacroDocument"
    - "LibraryPanel.set_dirty() is called whenever the editor's document_modified fires, so the unsaved-changes guard works"
    - "LibraryPanel.refresh() is called after every save operation so the file list stays current"
    - "Recording and stopping recording loads the new macro into the EditorPanel automatically"
  artifacts:
    - path: "src/macro_thunder/ui/main_window.py"
      provides: "Fully wired MainWindow connecting EditorPanel and LibraryPanel to macro buffer and file ops"
      min_lines: 250
  key_links:
    - from: "src/macro_thunder/ui/main_window.py"
      to: "src/macro_thunder/ui/editor_panel.py"
      via: "calls editor_panel.load_document(doc) after load and after record stop"
      pattern: "_editor_panel.load_document"
    - from: "src/macro_thunder/ui/main_window.py"
      to: "src/macro_thunder/ui/library_panel.py"
      via: "connects load_requested/save_requested signals; calls set_dirty/refresh"
      pattern: "_library_panel.load_requested|_library_panel.save_requested"
---

<objective>
Wire MainWindow to connect EditorPanel and LibraryPanel: load → editor shows blocks, save → library refreshes, record stop → editor loads new recording, dirty flag propagates for unsaved-changes guard. This is the integration layer that makes the full Phase 3 feature set visible to the user.

Purpose: All the components exist; this plan makes them talk to each other. Ends with a human verification checkpoint.
Output: Updated main_window.py; all Phase 3 features end-to-end functional.
</objective>

<execution_context>
@C:/Users/AMD/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/AMD/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-visual-block-editor/03-CONTEXT.md
@src/macro_thunder/ui/main_window.py
@src/macro_thunder/ui/editor_panel.py
@src/macro_thunder/ui/library_panel.py
@src/macro_thunder/persistence/serializer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire MainWindow — editor load, library signals, dirty flag, record-stop integration</name>
  <files>src/macro_thunder/ui/main_window.py</files>
  <action>
    Update main_window.py to integrate EditorPanel and LibraryPanel fully. The existing file already sets up _library_panel and _editor_panel as stub widgets — replace the wiring, not the layout.

    1. Add dirty flag tracking:
       self._is_dirty: bool = False

       After instantiating _editor_panel (in __init__):
         self._editor_panel.document_modified.connect(self._on_document_modified)

       def _on_document_modified(self):
           self._is_dirty = True
           self._library_panel.set_dirty(True)

    2. Update _load_document(doc: MacroDocument) helper (new private method):
       def _load_document(self, doc: MacroDocument) -> None:
           self._macro_buffer = doc
           self._is_dirty = False
           self._library_panel.set_dirty(False)
           self._editor_panel.load_document(doc)
           self.statusBar().showMessage(f"Loaded: {doc.name}", 3000)

    3. Update _stop_record to call _load_document:
       Replace the existing line:
         self._macro_buffer = MacroDocument(blocks=self._rec_blocks)
       With:
         doc = MacroDocument(blocks=self._rec_blocks)
         self._load_document(doc)

    4. Update _open_macro to call _load_document:
       Replace:
         self._macro_buffer = load_macro(pathlib.Path(path))
         self.statusBar().showMessage(...)
       With:
         doc = load_macro(pathlib.Path(path))
         self._load_document(doc)

    5. Update _save_macro to refresh library:
       After save_macro(self._macro_buffer, pathlib.Path(path)):
         self._is_dirty = False
         self._library_panel.set_dirty(False)
         self._library_panel.refresh()
       Also update self._macro_buffer.name if path.stem differs.

    6. Connect LibraryPanel signals (in __init__, after panels created):
       self._library_panel.load_requested.connect(self._on_library_load)
       self._library_panel.save_requested.connect(self._save_macro)

       def _on_library_load(self, path: str) -> None:
           doc = load_macro(pathlib.Path(path))
           self._load_document(doc)

    7. After save in _save_macro:
       self._library_panel.refresh()

    IMPORTANT: Do NOT remove any existing functionality (recording, playback, hotkeys, coord timer). Only add new wiring on top of what's there. Read the entire existing file before editing to understand all method names.

    After the editor_panel.load_document call, also update toolbar block count:
      self._toolbar_widget.update_block_count(len(doc.blocks))
  </action>
  <verify>
    <automated>cd C:/Users/AMD/Documents/programming/Macro-Thunder && python -m pytest -x</automated>
    <manual>Launch the app: `python -m macro_thunder`. Verify: File > Open loads a macro and the block table populates. Record a short macro, Stop, and the editor panel shows the recorded blocks. Library panel shows saved files.</manual>
  </verify>
  <done>MainWindow._load_document exists; _editor_panel.load_document called after open/record-stop; _library_panel.refresh called after save; _is_dirty + set_dirty wired correctly; all tests pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — full Phase 3 end-to-end</name>
  <what-built>
    Full Phase 3 feature set:
    - BlockTableModel with display rows (groups, individual blocks)
    - Expand/collapse group rows by clicking the arrow column
    - Inline editing of group duration and coordinates
    - Delete, Move Up, Move Down, Add Block toolbar buttons
    - Drag-and-drop row reorder
    - Multi-select (Shift+click, Ctrl+click)
    - Library panel with Load/Rename/Delete and MRU sorting
    - Unsaved-changes guard on library load
    - MainWindow wiring: record-stop loads editor, save refreshes library
  </what-built>
  <how-to-verify>
    Launch: `cd C:/Users/AMD/Documents/programming/Macro-Thunder && python -m macro_thunder`

    Test sequence:
    1. Press Record, move mouse a bit, press some keys, press Stop.
       EXPECT: Block editor table populates with rows. Mouse moves appear as a collapsed group row "▶ MouseMove Group — N moves ...".

    2. Click the ▶ arrow column on the group row.
       EXPECT: Group expands inline, showing child move rows indented below the header.

    3. Click the ▼ arrow to collapse.
       EXPECT: Child rows disappear, header shows ▶ again.

    4. Double-click the duration cell (3rd column) on the group header row.
       EXPECT: Inline editor appears. Type a new value (e.g. 2.0) and press Enter.
       EXPECT: Duration cell updates.

    5. Select a block row, press Delete button.
       EXPECT: Row is removed.

    6. Select two rows with Ctrl+click, press Delete.
       EXPECT: Both removed.

    7. Select a row, press Move Up or Move Down.
       EXPECT: Row moves one position.

    8. Click Add Block, select "Delay" from the dialog, press OK.
       EXPECT: A new Delay block appears after the selected row.

    9. Drag a row to a new position.
       EXPECT: Row moves to the dropped position.

    10. File > Save Macro. Save to ~/Documents/MacroThunder/test.json.
        EXPECT: Library panel shows "test" at top of list.

    11. Library panel: right-click "test", select Rename, type "test-renamed".
        EXPECT: Item renames in the list.

    12. Library panel: Load button on a different macro (or load test-renamed).
        EXPECT: Block editor refreshes with the loaded macro's blocks.

    13. Make an edit (delete a row), then click Load on a different library item.
        EXPECT: Prompt "Save changes before loading?" appears.
  </how-to-verify>
  <action>Human verification — no automated action. Run the app and follow the how-to-verify steps above.</action>
  <verify>
    <automated>cd C:/Users/AMD/Documents/programming/Macro-Thunder && python -m pytest -x</automated>
    <manual>Follow all 13 steps in how-to-verify above.</manual>
  </verify>
  <done>All 13 verification steps pass; no console errors; existing recording/playback still works</done>
  <resume-signal>Type "approved" if all steps pass, or describe any failures</resume-signal>
</task>

</tasks>

<verification>
Run full test suite: `cd C:/Users/AMD/Documents/programming/Macro-Thunder && python -m pytest -x`
</verification>

<success_criteria>
- All 13 verification steps pass
- No Python errors in console during testing
- Existing recording and playback still work (no regressions)
- All pytest tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-visual-block-editor/03-06-SUMMARY.md`
</output>
