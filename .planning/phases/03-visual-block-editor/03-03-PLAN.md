---
phase: 03-visual-block-editor
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/macro_thunder/ui/block_delegate.py
  - src/macro_thunder/ui/block_type_dialog.py
autonomous: true
requirements:
  - EDIT-05
  - GROUP-01

must_haves:
  truths:
    - "Group header rows render with a collapse arrow (▶/▼) visually distinguishable from regular block rows"
    - "BlockTypeDialog presents all 8 block types and returns a default-constructed ActionBlock instance when accepted"
    - "BlockDelegate.editorEvent detects clicks in the arrow column (COL_TYPE) on GroupHeaderRow and emits toggle_group_requested signal"
  artifacts:
    - path: "src/macro_thunder/ui/block_delegate.py"
      provides: "QStyledItemDelegate subclass for group row rendering and toggle detection"
      exports: ["BlockDelegate"]
    - path: "src/macro_thunder/ui/block_type_dialog.py"
      provides: "Modal dialog for choosing and inserting a new block type"
      exports: ["BlockTypeDialog"]
  key_links:
    - from: "src/macro_thunder/ui/block_delegate.py"
      to: "src/macro_thunder/models/view_model.py"
      via: "isinstance check on display row from model UserRole data"
      pattern: "GroupHeaderRow"
    - from: "src/macro_thunder/ui/block_type_dialog.py"
      to: "src/macro_thunder/models/blocks.py"
      via: "returns ActionBlock instances for each block type"
      pattern: "MouseMoveBlock|MouseClickBlock|KeyPressBlock|DelayBlock"
---

<objective>
Build the two UI support components that EditorPanel depends on: BlockDelegate (custom renderer for group rows + toggle detection) and BlockTypeDialog (block type picker for the "Add Block" toolbar button).

Purpose: Isolating these as separate files keeps EditorPanel focused on layout and wiring rather than rendering logic. Both are small, independent of each other, and needed by plan 03-05.
Output: block_delegate.py and block_type_dialog.py, importable and functional.
</objective>

<execution_context>
@C:/Users/AMD/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/AMD/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-visual-block-editor/03-CONTEXT.md
@.planning/phases/03-visual-block-editor/03-RESEARCH.md
@src/macro_thunder/models/blocks.py
@src/macro_thunder/models/view_model.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: BlockDelegate — group row rendering and toggle click detection</name>
  <files>src/macro_thunder/ui/block_delegate.py</files>
  <action>
    Create src/macro_thunder/ui/block_delegate.py.

    class BlockDelegate(QStyledItemDelegate):
      Signals:
        toggle_group_requested = pyqtSignal(int)  # display_row_index

      paint(self, painter, option, index):
        row_data = index.model().data(index, Qt.ItemDataRole.UserRole)  # model will expose row type
        If index.column() == COL_TYPE and isinstance(row_data, GroupHeaderRow):
          Draw the standard cell background (call super().paint() for base then overlay arrow)
          OR: just call super().paint() and let data() return the ▶/▼ prefix — no custom paint needed unless visual polish required.
          Decision (Claude's Discretion): Use data() text prefix approach (▶/▼ already in data()) — do NOT override paint(), keep delegate lightweight. Only override editorEvent.

      editorEvent(self, event, model, option, index):
        If event.type() == QEvent.Type.MouseButtonRelease and index.column() == COL_TYPE:
          row_data = model.data(index, Qt.ItemDataRole.UserRole)
          if isinstance(row_data, GroupHeaderRow):
            self.toggle_group_requested.emit(index.row())
            return True  # consume event, don't start editor
        return super().editorEvent(event, model, option, index)

    For UserRole data delivery: BlockTableModel.data() must return the DisplayRow object when role == Qt.ItemDataRole.UserRole. Add this to view_model.py (small addition: `if role == Qt.ItemDataRole.UserRole: return self._display_rows[index.row()]`).

    COL_TYPE = 0 — import from view_model or define locally as 0.

    Imports needed:
      from PyQt6.QtWidgets import QStyledItemDelegate
      from PyQt6.QtCore import Qt, QEvent, pyqtSignal
      from macro_thunder.models.view_model import GroupHeaderRow

    Note: QAction must come from PyQt6.QtGui (not QtWidgets) per CLAUDE.md.
  </action>
  <verify>
    <automated>cd C:/Users/AMD/Documents/programming/Macro-Thunder && python -c "from macro_thunder.ui.block_delegate import BlockDelegate; print('OK')"</automated>
    <manual>Import succeeds with no errors.</manual>
  </verify>
  <done>BlockDelegate importable; toggle_group_requested signal defined; editorEvent intercepts clicks on COL_TYPE for GroupHeaderRow rows</done>
</task>

<task type="auto">
  <name>Task 2: BlockTypeDialog — block type picker returning a default ActionBlock</name>
  <files>src/macro_thunder/ui/block_type_dialog.py</files>
  <action>
    Create src/macro_thunder/ui/block_type_dialog.py.

    class BlockTypeDialog(QDialog):
      Shows a list of block types the user can choose from. On OK, returns a default-constructed ActionBlock.

      Block types available (with user-friendly labels):
        "Mouse Move"     → MouseMoveBlock(x=0, y=0, timestamp=0.0)
        "Mouse Click"    → MouseClickBlock(x=0, y=0, button="left", direction="down", timestamp=0.0)
        "Mouse Scroll"   → MouseScrollBlock(x=0, y=0, dx=0, dy=1, timestamp=0.0)
        "Key Press"      → KeyPressBlock(key="a", direction="down", timestamp=0.0)
        "Delay"          → DelayBlock(duration=0.5)
        "Window Focus"   → WindowFocusBlock(executable="", title="", match_mode="Contains")
        "Label"          → LabelBlock(name="start")
        "Goto"           → GotoBlock(target="start")

      Layout: QListWidget with the 8 entries, OK/Cancel buttons (use QDialogButtonBox).
      Double-click on an item also accepts the dialog.

      static method get_block(parent=None) -> ActionBlock | None:
        dlg = BlockTypeDialog(parent)
        if dlg.exec() == QDialog.DialogCode.Accepted:
            return dlg._selected_block()
        return None

      _selected_block(self): Return the default block corresponding to the current QListWidget selection.

      Imports:
        from PyQt6.QtWidgets import QDialog, QVBoxLayout, QListWidget, QListWidgetItem, QDialogButtonBox
        from macro_thunder.models.blocks import (MouseMoveBlock, MouseClickBlock, MouseScrollBlock,
            KeyPressBlock, DelayBlock, WindowFocusBlock, LabelBlock, GotoBlock)
  </action>
  <verify>
    <automated>cd C:/Users/AMD/Documents/programming/Macro-Thunder && python -c "from macro_thunder.ui.block_type_dialog import BlockTypeDialog; print('OK')"</automated>
    <manual>Import succeeds. Dialog can be instantiated without error.</manual>
  </verify>
  <done>BlockTypeDialog importable; get_block() static method returns ActionBlock subclass or None; all 8 block types selectable</done>
</task>

</tasks>

<verification>
Run full test suite: `cd C:/Users/AMD/Documents/programming/Macro-Thunder && python -m pytest -x`
Import both new modules: `python -c "from macro_thunder.ui.block_delegate import BlockDelegate; from macro_thunder.ui.block_type_dialog import BlockTypeDialog; print('OK')"`
</verification>

<success_criteria>
- block_delegate.py and block_type_dialog.py both importable
- BlockDelegate.toggle_group_requested signal defined
- BlockTypeDialog.get_block() returns an ActionBlock subclass instance
- No existing tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/03-visual-block-editor/03-03-SUMMARY.md`
</output>
