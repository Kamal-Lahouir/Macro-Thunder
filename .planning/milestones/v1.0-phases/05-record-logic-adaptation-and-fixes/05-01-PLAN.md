---
phase: 05-record-logic-adaptation-and-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/macro_thunder/settings.py
  - src/macro_thunder/ui/settings_dialog.py
  - src/macro_thunder/ui/main_window.py
autonomous: true
requirements: [REC-05-CLICK, REC-05-RECHERE, REC-05-SETTINGS]

must_haves:
  truths:
    - "AppSettings loads from disk with new fields (click_mode, hotkey_record_here, sound_cue_enabled) and falls back to defaults on old settings files"
    - "SettingsDialog shows a Hotkeys tab (all 5 hotkeys including Record Here) and an Options tab (click mode, threshold, sound cue)"
    - "Entering a Record Here hotkey that matches an existing hotkey shows an error dialog and rejects the save"
    - "Settings menu item appears in the menu bar at top level (not inside File menu)"
  artifacts:
    - path: "src/macro_thunder/settings.py"
      provides: "AppSettings with click_mode, hotkey_record_here, sound_cue_enabled fields"
      contains: "hotkey_record_here"
    - path: "src/macro_thunder/ui/settings_dialog.py"
      provides: "QTabWidget-based SettingsDialog with Hotkeys + Options tabs and conflict detection"
      contains: "QTabWidget"
    - path: "src/macro_thunder/ui/main_window.py"
      provides: "Top-level Settings menu in menu bar"
      contains: "settings_menu"
  key_links:
    - from: "src/macro_thunder/ui/settings_dialog.py"
      to: "src/macro_thunder/settings.py"
      via: "AppSettings fields read/written in accept()"
      pattern: "hotkey_record_here|click_mode|sound_cue_enabled"
    - from: "src/macro_thunder/ui/main_window.py"
      to: "src/macro_thunder/ui/settings_dialog.py"
      via: "_open_settings() opens SettingsDialog"
      pattern: "SettingsDialog"
---

<objective>
Extend AppSettings with three new fields and restructure SettingsDialog into a tabbed layout.
Move Settings to a top-level menu. This creates the foundation other Phase 5 plans depend on.

Purpose: All Phase 5 features (click mode, Record Here hotkey, sound cue) need persisted settings fields and a unified settings UI.
Output: Updated settings.py, restructured settings_dialog.py, updated main_window.py menu bar.
</objective>

<execution_context>
@C:/Users/AMD/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/AMD/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/macro_thunder/settings.py
@src/macro_thunder/ui/settings_dialog.py
@src/macro_thunder/ui/main_window.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend AppSettings with click_mode, hotkey_record_here, sound_cue_enabled</name>
  <files>src/macro_thunder/settings.py</files>
  <action>
Add three new fields to the `AppSettings` dataclass (after `mouse_threshold_px`):

```python
click_mode: str = "separate"        # "separate" = down+up blocks, "combined" = single click block
hotkey_record_here: str = ""        # empty string = disabled; guarded in HotkeyManager
sound_cue_enabled: bool = False     # winsound.Beep on record start; off by default
```

The existing `load()` classmethod already uses `{k: v for k, v in data.items() if k in fields}` — this handles backwards compatibility automatically. The existing `save()` uses `dataclasses.asdict()` which picks up new fields automatically. No changes needed to load/save logic.

Verify the field names match exactly what `settings_dialog.py` (Task 2) will write:
- `click_mode` (str)
- `hotkey_record_here` (str)
- `sound_cue_enabled` (bool)
  </action>
  <verify>
    <automated>cd C:/Users/AMD/Documents/programming/Macro-Thunder && python -c "from macro_thunder.settings import AppSettings; s = AppSettings(); assert hasattr(s, 'click_mode'); assert s.click_mode == 'separate'; assert hasattr(s, 'hotkey_record_here'); assert s.hotkey_record_here == ''; assert hasattr(s, 'sound_cue_enabled'); assert s.sound_cue_enabled == False; print('AppSettings fields OK')"</automated>
  </verify>
  <done>AppSettings instantiates with all three new fields at their defaults without error.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure SettingsDialog to QTabWidget + add conflict detection + move Settings menu</name>
  <files>src/macro_thunder/ui/settings_dialog.py, src/macro_thunder/ui/main_window.py</files>
  <action>
**settings_dialog.py** — Full rewrite of the dialog layout:

Replace the flat `QFormLayout` with a `QTabWidget` containing two tabs. Add imports for `QTabWidget`, `QCheckBox`, `QComboBox`.

Layout structure:
```
QVBoxLayout
  QTabWidget
    Tab "Hotkeys" (QFormLayout):
      "Start Record:" -> _edit_start_record (pre-filled from settings)
      "Stop Record:"  -> _edit_stop_record
      "Start Playback:" -> _edit_start_play
      "Stop Playback:"  -> _edit_stop_play
      "Record Here:"  -> _edit_record_here (pre-filled from settings.hotkey_record_here; hint: leave blank to disable)
      QLabel "Hotkey format: <f8>, <ctrl>+a, a  |  Leave Record Here blank to disable"
        (style: gray, font-size 11px)
    Tab "Options" (QFormLayout):
      "Click mode:"   -> _combo_click_mode (QComboBox, items: ["Separate (down + up)", "Combined (single click)"])
                         index 0 = "separate", index 1 = "combined"
                         pre-select based on settings.click_mode
      "Mouse threshold (px):" -> _spin_threshold (range 0–20, existing)
      "Sound cue on record:"  -> _chk_sound_cue (QCheckBox, checked = settings.sound_cue_enabled)
  QDialogButtonBox (OK | Cancel)
```

**accept() method** — Replace current accept with:
1. Read `record_here = self._edit_record_here.text().strip()`
2. If `record_here` is non-empty, check against existing hotkeys:
   ```python
   existing = {
       self._edit_start_record.text().strip(): "Start Record",
       self._edit_stop_record.text().strip(): "Stop Record",
       self._edit_start_play.text().strip(): "Start Playback",
       self._edit_stop_play.text().strip(): "Stop Playback",
   }
   if record_here in existing:
       QMessageBox.warning(self, "Hotkey Conflict",
           f"'{record_here}' is already assigned to: {existing[record_here]}")
       return  # Do NOT call super().accept()
   ```
3. Write all settings fields:
   - `self._settings.hotkey_start_record = self._edit_start_record.text().strip()`
   - `self._settings.hotkey_stop_record = ...`
   - `self._settings.hotkey_start_play = ...`
   - `self._settings.hotkey_stop_play = ...`
   - `self._settings.hotkey_record_here = record_here`
   - `self._settings.mouse_threshold_px = self._spin_threshold.value()`
   - `self._settings.click_mode = "combined" if self._combo_click_mode.currentIndex() == 1 else "separate"`
   - `self._settings.sound_cue_enabled = self._chk_sound_cue.isChecked()`
4. Call `self._settings.save()`
5. Call `super().accept()`

**main_window.py** — Move Settings from File menu to top-level menu:

Find the block that adds settings to File menu (something like `file_menu.addAction(settings_action)`). Remove or comment out that line. After `file_menu = self.menuBar().addMenu(...)`, add:

```python
settings_menu = self.menuBar().addMenu("&Settings")
settings_action = QAction("&Preferences...", self)
settings_action.triggered.connect(self._open_settings)
settings_menu.addAction(settings_action)
```

The `_open_settings` method already exists — no change needed there.

Import `QMessageBox` in settings_dialog.py if not already imported.
  </action>
  <verify>
    <automated>cd C:/Users/AMD/Documents/programming/Macro-Thunder && python -c "
from PyQt6.QtWidgets import QApplication
import sys
app = QApplication(sys.argv)
from macro_thunder.settings import AppSettings
from macro_thunder.ui.settings_dialog import SettingsDialog
s = AppSettings()
dlg = SettingsDialog(s)
# Verify tab widget exists
from PyQt6.QtWidgets import QTabWidget
tabs = dlg.findChildren(QTabWidget)
assert len(tabs) == 1, f'Expected 1 QTabWidget, got {len(tabs)}'
assert tabs[0].count() == 2, f'Expected 2 tabs, got {tabs[0].count()}'
assert tabs[0].tabText(0) == 'Hotkeys'
assert tabs[0].tabText(1) == 'Options'
print('SettingsDialog QTabWidget OK')
"</automated>
    <manual>Launch the app and open Settings from the menu bar — verify it appears as a top-level "Settings" menu item, the dialog shows "Hotkeys" and "Options" tabs, and the Hotkeys tab has a "Record Here" field.</manual>
  </verify>
  <done>SettingsDialog opens with two tabs; entering a conflicting hotkey in Record Here shows an error; Settings appears in menu bar at top level.</done>
</task>

</tasks>

<verification>
Run full test suite: `cd C:/Users/AMD/Documents/programming/Macro-Thunder && python -m pytest tests/ -x -q`
Verify no import errors: `python -c "from macro_thunder.ui.settings_dialog import SettingsDialog; from macro_thunder.settings import AppSettings; print('imports OK')"`
</verification>

<success_criteria>
- AppSettings has click_mode="separate", hotkey_record_here="", sound_cue_enabled=False as defaults
- Old settings.json files load without error (backwards compatible)
- SettingsDialog shows Hotkeys tab (5 hotkeys) and Options tab (click mode, threshold, sound cue)
- Hotkey conflict in Record Here field shows QMessageBox.warning and blocks save
- Settings menu is top-level in menu bar, not nested in File menu
</success_criteria>

<output>
After completion, create `.planning/phases/05-record-logic-adaptation-and-fixes/05-01-SUMMARY.md`
</output>
