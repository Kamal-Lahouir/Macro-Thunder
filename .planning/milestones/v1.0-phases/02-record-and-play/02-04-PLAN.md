---
phase: 02-record-and-play
plan: 04
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - src/macro_thunder/ui/main_window.py
  - src/macro_thunder/ui/settings_dialog.py
  - src/macro_thunder/__main__.py
autonomous: false
requirements: [REC-01, REC-03, REC-04, REC-05, REC-06, REC-07, PLAY-01, PLAY-03, PLAY-04, PLAY-05]

must_haves:
  truths:
    - "User presses Record (button or F9), moves mouse and types keys, presses Stop Record (button or F10) — a MacroDocument is in memory"
    - "User presses Play (button or F6) — the recorded actions replay on screen with accurate timing"
    - "User presses F8 while another app has focus — playback halts after the in-flight event"
    - "UI does not freeze during recording or playback (toolbar updates and cursor readout remain live)"
    - "File > Save writes the in-memory macro to Documents/MacroThunder/<name>.json; File > Open loads one back"
    - "Settings dialog lets user change all 4 hotkeys and pixel threshold; changes take effect immediately"
  artifacts:
    - path: "src/macro_thunder/ui/main_window.py"
      provides: "MainWindow wired with RecorderService, PlaybackEngine, HotkeyManager, ToolbarPanel signals"
      exports: ["MainWindow"]
    - path: "src/macro_thunder/ui/settings_dialog.py"
      provides: "SettingsDialog: hotkey + threshold config, saves AppSettings on accept"
      exports: ["SettingsDialog"]
  key_links:
    - from: "src/macro_thunder/ui/main_window.py"
      to: "src/macro_thunder/recorder/__init__.py"
      via: "RecorderService(queue=self._rec_queue, threshold=settings.mouse_threshold_px)"
      pattern: "RecorderService"
    - from: "src/macro_thunder/ui/main_window.py"
      to: "src/macro_thunder/engine/__init__.py"
      via: "PlaybackEngine(on_progress=self._on_progress)"
      pattern: "PlaybackEngine"
    - from: "src/macro_thunder/ui/main_window.py"
      to: "src/macro_thunder/hotkeys.py"
      via: "HotkeyManager signals connected to _start_record / _stop_record / _start_play / _stop_play slots"
      pattern: "HotkeyManager"
    - from: "src/macro_thunder/ui/main_window.py"
      to: "src/macro_thunder/persistence/__init__.py"
      via: "save_macro / load_macro for File > Save / File > Open"
      pattern: "save_macro|load_macro"
---

<objective>
Wire all Phase 2 components into the MainWindow and add File menu + Settings dialog.

Purpose: This plan closes the end-to-end loop. The recorder, engine, hotkey manager, and toolbar are all built; this plan connects them through an AppState machine in MainWindow and adds persistence (File menu) and hotkey configuration (Settings dialog).

Output: A fully functional record-and-play application where the user can record, save, load, and play macros.
</objective>

<execution_context>
@C:/Users/AMD/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/AMD/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-record-and-play/02-CONTEXT.md
@.planning/phases/02-record-and-play/02-01-SUMMARY.md
@.planning/phases/02-record-and-play/02-02-SUMMARY.md
@.planning/phases/02-record-and-play/02-03-SUMMARY.md
@src/macro_thunder/ui/main_window.py
@src/macro_thunder/__main__.py
@src/macro_thunder/persistence/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SettingsDialog</name>
  <files>src/macro_thunder/ui/settings_dialog.py</files>
  <action>
    Create SettingsDialog(QDialog):

    Layout: QFormLayout inside QVBoxLayout with OK/Cancel buttons.

    Form rows:
    - "Start Record:" QLineEdit(text=settings.hotkey_start_record) → _edit_start_record
    - "Stop Record:" QLineEdit(text=settings.hotkey_stop_record) → _edit_stop_record
    - "Start Playback:" QLineEdit(text=settings.hotkey_start_play) → _edit_start_play
    - "Stop Playback:" QLineEdit(text=settings.hotkey_stop_play) → _edit_stop_play
    - "Mouse threshold (px):" QSpinBox(min=0, max=20, value=settings.mouse_threshold_px) → _spin_threshold

    Constructor: SettingsDialog(settings: AppSettings, parent=None)
    - Populates fields from settings
    - Sets window title "Settings"
    - Adds small help label: "Hotkey format: <f8>, <ctrl>+a, a"

    accept() override (or OK button clicked):
    - Updates settings fields from widget values
    - Calls settings.save()
    - Calls super().accept()

    get_settings() -> AppSettings:
    - Returns the modified settings object (caller can use to re-register hotkeys)

    No validation required — if user enters invalid hotkey string, pynput will raise on
    register; catch that in MainWindow and show QMessageBox.warning().
  </action>
  <verify>
    <automated>python -c "from macro_thunder.ui.settings_dialog import SettingsDialog; print('OK')"</automated>
  </verify>
  <done>SettingsDialog imports cleanly and has all 5 form fields and OK/Cancel buttons.</done>
</task>

<task type="auto">
  <name>Task 2: Wire MainWindow — AppState machine + File menu + component connections</name>
  <files>src/macro_thunder/ui/main_window.py, src/macro_thunder/__main__.py</files>
  <action>
    **AppState enum** (define at top of main_window.py):
    ```python
    import enum
    class AppState(enum.Enum):
        IDLE = "idle"
        RECORDING = "recording"
        PLAYING = "playing"
    ```

    **MainWindow.__init__ additions** (extend existing __init__, do not rewrite from scratch):

    1. Load settings:
       self._settings = AppSettings.load()

    2. Create services:
       self._rec_queue: queue.Queue = queue.Queue()
       self._recorder = RecorderService(self._rec_queue, self._settings.mouse_threshold_px)
       self._engine = PlaybackEngine(on_progress=self._on_play_progress)
       self._macro_buffer: MacroDocument | None = None
       self._state: AppState = AppState.IDLE

    3. Create HotkeyManager and register:
       self._hotkeys = HotkeyManager(self)
       try:
           self._hotkeys.register(self._settings)
       except Exception as e:
           QMessageBox.warning(self, "Hotkey Error", str(e))

    4. Connect toolbar signals:
       toolbar.record_requested.connect(self._start_record)
       toolbar.stop_record_requested.connect(self._stop_record)
       toolbar.play_requested.connect(self._start_play)
       toolbar.stop_play_requested.connect(self._stop_play)

    5. Connect hotkey signals:
       self._hotkeys.start_record.connect(self._start_record)
       self._hotkeys.stop_record.connect(self._stop_record)
       self._hotkeys.start_play.connect(lambda: self._start_play(self._settings_speed(), 1))
       self._hotkeys.stop_play.connect(self._stop_play)

    6. Recording drain timer (for recorder queue → UI update):
       self._rec_drain_timer = QTimer(self)
       self._rec_drain_timer.setInterval(50)  # 50ms drain — 20 Hz is enough for block count
       self._rec_drain_timer.timeout.connect(self._drain_recorder)
       (Do NOT start until _start_record)

    **Slot implementations:**

    _start_record():
        if self._state != AppState.IDLE: return
        self._state = AppState.RECORDING
        self._rec_blocks: list = []
        self._recorder.start()
        self._rec_drain_timer.start()
        toolbar.set_recording(True, 0)

    _stop_record():
        if self._state != AppState.RECORDING: return
        self._recorder.stop()
        self._rec_drain_timer.stop()
        # Drain remaining events
        self._drain_recorder()
        self._macro_buffer = MacroDocument(blocks=self._rec_blocks)
        self._state = AppState.IDLE
        toolbar.set_recording(False)
        toolbar.update_block_count(0)

    _drain_recorder():
        while not self._rec_queue.empty():
            block = self._rec_queue.get_nowait()
            self._rec_blocks.append(block)
            toolbar.update_block_count(len(self._rec_blocks))

    _start_play(speed: float, repeat: int):
        if self._state != AppState.IDLE: return
        if self._macro_buffer is None or not self._macro_buffer.blocks:
            QMessageBox.information(self, "No Macro", "Record or load a macro first.")
            return
        self._state = AppState.PLAYING
        toolbar.set_playback(True)
        self._engine.start(self._macro_buffer.blocks, speed=speed, repeat=repeat)

    _stop_play():
        self._engine.stop()
        self._state = AppState.IDLE
        toolbar.set_playback(False)

    _on_play_progress(index: int, total: int):
        # Called from playback thread — use queue+QTimer (existing pattern)
        # Add to a separate _play_progress_queue and drain via existing status timer
        # OR: use QMetaObject.invokeMethod for thread-safe slot invocation:
        # Use QMetaObject.invokeMethod(self, "_update_progress", Qt.ConnectionType.QueuedConnection,
        #                              Q_ARG(int, index), Q_ARG(int, total))
        # Simpler approach: queue.Queue + existing 16ms status bar timer
        # Use self._play_progress_queue (add in __init__) and drain in the existing _update_status timer slot
        self._play_progress_queue.put((index, total))

    Add self._play_progress_queue = queue.Queue() in __init__.
    In the existing QTimer slot that updates the status bar (_update_status or similar):
        while not self._play_progress_queue.empty():
            idx, total = self._play_progress_queue.get_nowait()
            toolbar.set_playback_progress(idx, total)
            if idx >= total:
                self._stop_play()  # auto-reset when playback finishes

    **File menu** (add to existing menuBar() in MainWindow):
    file_menu = menuBar().addMenu("&File")

    save_action = QAction("&Save Macro...", self)
    save_action.setShortcut("Ctrl+S")
    save_action.triggered.connect(self._save_macro)
    file_menu.addAction(save_action)

    open_action = QAction("&Open Macro...", self)
    open_action.setShortcut("Ctrl+O")
    open_action.triggered.connect(self._open_macro)
    file_menu.addAction(open_action)

    file_menu.addSeparator()

    settings_action = QAction("&Settings...", self)
    settings_action.triggered.connect(self._open_settings)
    file_menu.addAction(settings_action)

    _save_macro():
        if self._macro_buffer is None:
            QMessageBox.information(self, "Nothing to Save", "No macro in memory.")
            return
        path, _ = QFileDialog.getSaveFileName(
            self, "Save Macro", str(SETTINGS_DIR), "Macro Files (*.json)"
        )
        if path:
            save_macro(self._macro_buffer, pathlib.Path(path))

    _open_macro():
        path, _ = QFileDialog.getOpenFileName(
            self, "Open Macro", str(SETTINGS_DIR), "Macro Files (*.json)"
        )
        if path:
            self._macro_buffer = load_macro(pathlib.Path(path))
            self.statusBar().showMessage(f"Loaded: {pathlib.Path(path).name}", 3000)

    _open_settings():
        dlg = SettingsDialog(self._settings, self)
        if dlg.exec():
            self._settings = dlg.get_settings()
            try:
                self._hotkeys.register(self._settings)
            except Exception as e:
                QMessageBox.warning(self, "Hotkey Error", str(e))

    **Import additions to main_window.py:**
    from macro_thunder.recorder import RecorderService
    from macro_thunder.engine import PlaybackEngine
    from macro_thunder.hotkeys import HotkeyManager
    from macro_thunder.settings import AppSettings, SETTINGS_DIR
    from macro_thunder.models.document import MacroDocument
    from macro_thunder.persistence import save_macro, load_macro
    from macro_thunder.ui.settings_dialog import SettingsDialog
    import queue, pathlib

    **__main__.py**: No changes needed unless AppSettings load needs to happen before QApplication.
    Verify the existing entry point still works cleanly with the new imports.

    NOTE: The existing 16ms QTimer that polls QCursor.pos() for the status bar already exists in
    MainWindow from Phase 1. Extend its slot to also drain _play_progress_queue. Do not add a
    third timer — reuse the existing one.
  </action>
  <verify>
    <automated>pytest tests/ -v --tb=short</automated>
    <manual>
      1. Run `python -m macro_thunder`
      2. Press F9 (or Record button) — verify toolbar shows red blinking indicator and "0 blocks"
      3. Move mouse around, press a few keys — verify block count increments
      4. Press F10 (or Stop button) — verify indicator stops blinking, block count resets
      5. Press F6 (or Play button) — verify mouse and keyboard actions replay
      6. During playback, press F8 from another window — verify playback halts, progress clears
      7. File > Save Macro — save to Documents/MacroThunder/test.json; verify file created
      8. File > Open Macro — load it back; press Play — verify same actions replay
      9. File > Settings — change threshold; verify no crash on OK
    </manual>
  </verify>
  <done>
    Full record-play loop works end-to-end. F8 stops playback from any window. File Save/Load works.
    Settings dialog opens and saves. No UI freeze during recording or playback.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 2 record-and-play pipeline: RecorderService (TDD), PlaybackEngine (TDD), ToolbarPanel, HotkeyManager, AppSettings, SettingsDialog, MainWindow wiring, File menu.</what-built>
  <how-to-verify>
    1. `python -m macro_thunder` — app opens, toolbar has Record/Stop/Play/Stop buttons + speed controls
    2. Press F9 (or Record button) — red blinking indicator appears, block count starts at 0
    3. Move the mouse in a pattern (e.g. a circle) and press a letter key — block count increments
    4. Press F10 (or Stop Record button) — recording stops, indicator disappears
    5. Press F6 (or Play button) — mouse retraces the recorded path; key is typed
    6. While playing, switch to another window and press F8 — playback stops; UI resets to idle
    7. File > Save Macro, save as "test.json" in Documents/MacroThunder/
    8. Restart the app; File > Open Macro, load "test.json"; press Play — same actions replay
    9. File > Settings — verify hotkey fields and threshold spinbox visible; click OK, no crash

    Expected: All 9 steps pass without error or freeze.
  </how-to-verify>
  <resume-signal>Type "approved" if all 9 steps pass, or describe any failures.</resume-signal>
</task>

</tasks>

<verification>
Full test suite passes:
```
pytest tests/ -v --tb=short
```
Application runs end-to-end per the checkpoint verification steps above.
</verification>

<success_criteria>
- pytest tests/ passes (all existing + new tests from plans 01 and 02)
- Record → Stop → Play cycle works with mouse and keyboard
- F8 global stop works when another app has focus
- File > Save and File > Open round-trip a macro
- Settings dialog persists hotkey changes to settings.json
- No Qt crashes or thread violations (no "QObject: Cannot create children" errors in console)
</success_criteria>

<output>
After completion, create `.planning/phases/02-record-and-play/02-04-SUMMARY.md`
</output>
