---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/macro_thunder/__main__.py
  - src/macro_thunder/ui/main_window.py
  - src/macro_thunder/ui/toolbar.py
  - src/macro_thunder/ui/library_panel.py
  - src/macro_thunder/ui/editor_panel.py
autonomous: false
requirements:
  - FOUND-01
  - FOUND-04

must_haves:
  truths:
    - "Application launches to a dark-themed main window with no visible errors"
    - "Mouse coordinates displayed in status bar match OS screen coordinates at 100% and 125%+ display scaling"
    - "Three panel areas are visible: toolbar at top, library panel left, editor panel center-right"
    - "Live coordinate readout updates as the mouse moves anywhere over the app window"
  artifacts:
    - path: "src/macro_thunder/__main__.py"
      provides: "Entry point with DPI awareness set before any Qt import"
      contains: "SetProcessDpiAwareness"
    - path: "src/macro_thunder/ui/main_window.py"
      provides: "QMainWindow with 3-panel QSplitter layout and status bar coord label"
      exports:
        - "MainWindow"
    - path: "src/macro_thunder/ui/toolbar.py"
      provides: "Toolbar placeholder widget"
      exports:
        - "ToolbarPanel"
    - path: "src/macro_thunder/ui/library_panel.py"
      provides: "Left library panel placeholder"
      exports:
        - "LibraryPanel"
    - path: "src/macro_thunder/ui/editor_panel.py"
      provides: "Center/right editor panel placeholder"
      exports:
        - "EditorPanel"
  key_links:
    - from: "src/macro_thunder/__main__.py"
      to: "ctypes.windll.shcore.SetProcessDpiAwareness"
      via: "ctypes call before any Qt import"
      pattern: "SetProcessDpiAwareness"
    - from: "src/macro_thunder/ui/main_window.py"
      to: "QCursor.pos()"
      via: "mouseMoveEvent status bar update"
      pattern: "QCursor\\.pos\\(\\)"
    - from: "src/macro_thunder/ui/main_window.py"
      to: "src/macro_thunder/ui/library_panel.py"
      via: "QSplitter.addWidget()"
      pattern: "LibraryPanel"
---

<objective>
Implement the application entry point with correct DPI awareness ordering, and the dark-themed main window with a full three-panel layout skeleton (toolbar top, library left, editor center-right) and a live mouse coordinate readout in the status bar.

Purpose: This is the visual shell that proves DPI awareness is working (coordinates match OS at 125%+) and establishes the UI structure all later phases fill in without restructuring.
Output: A runnable, dark-themed application with live coordinate display.
</objective>

<execution_context>
@C:/Users/AMD/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/AMD/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/1-RESEARCH.md
@CLAUDE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement __main__.py entry point with DPI awareness</name>
  <files>src/macro_thunder/__main__.py</files>
  <action>
Implement `src/macro_thunder/__main__.py` exactly following RESEARCH.md Pattern 1 and the full entry point code example from RESEARCH.md.

CRITICAL ordering (per CLAUDE.md):
1. `import ctypes` — stdlib, safe
2. `ctypes.windll.shcore.SetProcessDpiAwareness(2)` wrapped in `try/except (AttributeError, OSError): pass`
3. ONLY THEN: all other imports (`import sys`, `from PyQt6...`, `from macro_thunder.ui...`)

Dark theme application (per RESEARCH.md Pattern 5):
- After `app = QApplication(sys.argv)`, try `import qdarktheme; qdarktheme.setup_theme("dark")`
- Except `ImportError`: call `_apply_fallback_dark_palette(app)` (manual QPalette fallback)
- The fallback palette MUST set Inactive color group for Window, Base, and Button to prevent white flash on focus loss

The `_apply_fallback_dark_palette()` function is a module-level private function defined below `main()`.

Full implementation from RESEARCH.md Code Examples section, with no deviations.

Do NOT use `QApplication.setAttribute(Qt.AA_EnableHighDpiScaling)` — this attribute was removed in Qt6 and raises AttributeError.
  </action>
  <verify>
    <automated>cd "C:/Users/AMD/Documents/programming/Macro-Thunder" && python -c "import ast, pathlib; src = pathlib.Path('src/macro_thunder/__main__.py').read_text(); tree = ast.parse(src); first_call = [n for n in ast.walk(tree) if isinstance(n, ast.Call)][0]; print('DPI check: OK' if 'SetProcessDpiAwareness' in src else 'FAIL: DPI call missing')"</automated>
    <manual>Verify `SetProcessDpiAwareness` appears before any `from PyQt6` or `import PyQt6` line in the file</manual>
  </verify>
  <done>
    `src/macro_thunder/__main__.py` exists. `SetProcessDpiAwareness(2)` call appears before any PyQt6 import. Both qdarktheme and QPalette fallback dark theme paths are present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement main_window.py and panel placeholder widgets</name>
  <files>
    src/macro_thunder/ui/main_window.py
    src/macro_thunder/ui/toolbar.py
    src/macro_thunder/ui/library_panel.py
    src/macro_thunder/ui/editor_panel.py
  </files>
  <action>
Implement all four UI files.

**`src/macro_thunder/ui/toolbar.py`** — Placeholder QFrame that future phases will populate:
```python
from PyQt6.QtWidgets import QFrame, QLabel, QHBoxLayout

class ToolbarPanel(QFrame):
    def __init__(self, parent=None):
        super().__init__(parent)
        layout = QHBoxLayout(self)
        layout.addWidget(QLabel("Toolbar — Phase 2"))
        self.setFixedHeight(40)
```

**`src/macro_thunder/ui/library_panel.py`** — Left panel placeholder:
```python
from PyQt6.QtWidgets import QFrame, QLabel, QVBoxLayout

class LibraryPanel(QFrame):
    def __init__(self, parent=None):
        super().__init__(parent)
        layout = QVBoxLayout(self)
        layout.addWidget(QLabel("Library — Phase 3"))
        self.setMinimumWidth(180)
        self.setMaximumWidth(320)
```

**`src/macro_thunder/ui/editor_panel.py`** — Center/right panel placeholder:
```python
from PyQt6.QtWidgets import QFrame, QLabel, QVBoxLayout

class EditorPanel(QFrame):
    def __init__(self, parent=None):
        super().__init__(parent)
        layout = QVBoxLayout(self)
        layout.addWidget(QLabel("Block Editor — Phase 3"))
```

**`src/macro_thunder/ui/main_window.py`** — QMainWindow with the full skeleton per RESEARCH.md Pattern 2 and Pattern 6:
- `QToolBar` added via `addToolBar()` at the top (contains ToolbarPanel or is populated in Phase 2)
- `QSplitter(Qt.Orientation.Horizontal)` as central widget container
- `LibraryPanel` on left, `EditorPanel` on right; stretch factor 0/1
- Status bar with a `QLabel` for coordinates: `self._coord_label = QLabel("X: 0  Y: 0")`; added via `self.statusBar().addPermanentWidget(self._coord_label)`
- `setMouseTracking(True)` on the main window
- `mouseMoveEvent` override: uses `QCursor.pos()` (NOT `event.position()`) for screen-absolute coordinates; updates `self._coord_label.setText(f"X: {pos.x()}  Y: {pos.y()}")`
- Window title: "Macro Thunder"
- Initial size: `self.resize(1200, 700)`

IMPORTANT per RESEARCH.md Pattern 6 and anti-patterns:
- Use `QCursor.pos()` for the coordinate label — this gives screen-absolute DPI-correct coordinates
- Enable `setMouseTracking(True)` on the splitter/central widget too so move events reach the window during mouse movement over child widgets
- Do NOT use `event.position()` for the coordinate display — it gives widget-local coordinates

Also call `self.setCentralWidget(self._splitter)` so the splitter fills the window.
  </action>
  <verify>
    <automated>cd "C:/Users/AMD/Documents/programming/Macro-Thunder" && python -c "from macro_thunder.ui.main_window import MainWindow; from macro_thunder.ui.library_panel import LibraryPanel; from macro_thunder.ui.editor_panel import EditorPanel; print('UI imports OK')"</automated>
    <manual>This verify is automated-only for import checking; the visual checkpoint below handles the actual UI verification</manual>
  </verify>
  <done>
    All four UI files exist. `from macro_thunder.ui.main_window import MainWindow` succeeds. `MainWindow` contains `QCursor.pos()` for coordinate display, `QSplitter` layout, and status bar label.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify dark-themed window launches with live coordinate display</name>
  <action>Run `python -m macro_thunder` and visually verify the application.</action>
  <what-built>
    Complete Phase 1 app shell: DPI-aware entry point, dark-themed main window with three-panel layout (toolbar top, library left, editor center-right), and live mouse coordinate readout in the status bar.
  </what-built>
  <how-to-verify>
    1. Run the application: `python -m macro_thunder`
    2. Confirm the window opens with a dark background (near-black, not medium gray)
    3. Confirm three areas are visible: a toolbar row at top, a narrower panel on the left, a wider panel on the right
    4. Move the mouse over the application window and confirm the status bar at the bottom shows live X and Y coordinates updating
    5. If your display is at 125%, 150%, or 200% scaling: move the cursor to a known position (e.g. top-left corner of your primary monitor at absolute 0,0) and confirm the coordinates shown match what the OS reports (Windows cursor position tool or similar)
    6. Confirm no error dialogs appear and no exceptions are printed in the terminal
  </how-to-verify>
  <resume-signal>Type "approved" if the window is dark-themed with live coordinates, or describe any issues seen</resume-signal>
</task>

</tasks>

<verification>
1. `python -c "from macro_thunder.ui.main_window import MainWindow"` — no errors
2. `python -c "import pathlib; src = pathlib.Path('src/macro_thunder/__main__.py').read_text(); assert src.index('SetProcessDpiAwareness') < src.index('from PyQt6')"` — passes (DPI before Qt)
3. Visual: `python -m macro_thunder` launches dark window with coordinate readout
</verification>

<success_criteria>
Application launches with `python -m macro_thunder`. Window is dark-themed. Three panel areas are visible. Status bar shows live X/Y coordinates updating as the mouse moves. No exceptions printed to terminal.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md` using the summary template.
</output>
